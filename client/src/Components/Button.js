import React from 'react'

function Button() {

    const handler=()=>{
        
    }
    return (
        <div>
            <button onClick={handler}></button>
        </div>
    )
}

export default Button